<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".MainApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <meta-data android:name="asset_statements" android:resource="@string/asset_statements" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/custom_url_scheme" />
            </intent-filter>

            <!-- App Links для возврата после внешней оплаты YooKassa -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="my.mentala.app" android:pathPrefix="/payment-success" />
                <data
                    android:scheme="https"
                    android:host="my.mentala.app"
                    android:pathPrefix="/auth/external-session/consume"
                />
                <data android:scheme="https" android:host="mentala.app" android:pathPrefix="/payment-success" />
                <data
                    android:scheme="https"
                    android:host="mentala.app"
                    android:pathPrefix="/auth/external-session/consume"
                />
            </intent-filter>
        </activity>

        <!-- Отключаем сервис плагина, чтобы не было двух обработчиков FCM -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            android:exported="false"
            tools:node="remove"
        />

        <!-- Firebase library тоже регистрирует базовый MessagingService.
             Его тоже убираем, иначе доставка MESSAGING_EVENT становится недетерминированной:
             часть push может уйти мимо нашего кастомного сервиса. -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false"
            tools:node="remove"
        />

        <!-- Наш FCM сервис: сам строит уведомления и гарантирует click intent -->
        <service android:name=".MentalaMessagingService" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- MediaGrid AudioPlayer service для background playback и системных media controls -->
        <service
            android:name="us.mediagrid.capacitorjs.plugins.nativeaudio.AudioPlayerService"
            android:description="@string/audio_player_service_description"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Отдельный foreground service для realtime voice при lock-screen -->
        <service
            android:name=".realtime.MentalaRealtimeVoiceForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|microphone"
        />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id"
        />
    </application>

    <!-- Permissions -->

    <!-- Явно удаляем рекламный идентификатор: приложение не использует рекламу -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Realtime voice: нужно для setSpeakerphoneOn / startBluetoothSco / setMode на API < 31 и для setCommunicationDevice на API 31+ -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Realtime voice: чтобы getAvailableCommunicationDevices() / getDevices() возвращали Bluetooth-устройства на Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Bluetooth-permissions для Android 8-11 (API < 31) — на API 31+ system игнорирует, заменяя BLUETOOTH_CONNECT -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
</manifest>
