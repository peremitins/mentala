// unplugin-icons is configured via Vite in nuxt.config.ts (autoInstall)
// This file can be used for future global icon registration if needed.
import { defineNuxtPlugin } from 'nuxt/app'
export default defineNuxtPlugin(() => {})
