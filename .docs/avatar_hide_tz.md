# ТЗ: Скрытие функционала аватара на главном экране

## Цель

Скрыть функционал аватара на главном экране, сохранив всю кодовую базу (компоненты, stores, API endpoints) для возможного использования в будущем.

## Принцип

- **Удаляется**: UI элементы, упоминания в интерфейсе, вызовы API запросов
- **Остается**: Компоненты, stores, API endpoints, логика в коде (но не вызывается)

---

## 1. UI элементы (удалить/скрыть)

### 1.1. Компонент `AvatarVoiceControls.vue`

**Файл**: `app/components/AvatarVoiceControls.vue`

**Изменения**:

- **Скрыть кнопку управления аватаром** (строки 3-23) - использовать `v-if="false"` или `style="display: none"`
- **Оставить кнопку управления звуком** - она остается видимой и рабочей
- **Компонент полностью остается** в проекте и используется на главной странице
- Весь код функции `toggleAvatar()` остается (строки 63-81), но не будет вызываться, так как кнопка скрыта

**Что скрыть**:

```vue
<!-- Кнопка аватара -->
<button
  v-if="false"  <!-- или style="display: none" -->
  @click="toggleAvatar"
  ...>
  ...
</button>
```

**Что оставить**:

- Весь код функции `toggleAvatar()` (строки 63-81)
- Все импорты и зависимости
- Кнопку звука (полностью рабочая)
- Структуру компонента

---

### 1.2. Главная страница `index.vue`

**Файл**: `app/pages/index.vue`

**Изменения**:

#### 1.2.1. Компонент `AvatarVoiceControls` остается на странице

**Строка 56**: Компонент остается, но внутри него скрыта кнопка аватара

```vue
<!-- Кнопки управления аватаром и звуком -->
<AvatarVoiceControls />
```

**Примечание**: Компонент остается на странице, но кнопка аватара скрыта внутри него (см. п. 1.1). Кнопка звука остается видимой и рабочей.

#### 1.2.2. Убрать отображение `HeyGenPlayer`

**Строки 75-91**: Удалить условный рендеринг аватара

```vue
<div v-if="chatSettings.avatar" ...>
  <HeyGenPlayer />
</div>
<div v-else ...>
  <div class="text-sm text-muted-foreground">
    <!-- Аватар не включен -->
  </div>
</div>
```

**Заменить на**: Просто пустой блок или убрать секцию полностью

#### 1.2.3. Убрать класс `chat-fade` связанный с аватаром

**Строка 98**: Удалить условие `:class="{ 'chat-fade': heygen.isConnected }"`

#### 1.2.4. Убрать комментарии об аватаре

**Строка 55**: Удалить комментарий `<!-- Кнопки управления аватаром и звуком -->`
**Строка 88**: Удалить комментарий `<!-- Аватар не включен -->`

**Что оставить**:

- Компонент `AvatarVoiceControls` на странице (строка 56) - остается, но кнопка аватара скрыта внутри
- Импорт `AvatarVoiceControls` (строка 181)
- Импорт `HeyGenPlayer` (строка 182)
- Импорт `useHeygenStore` (строка 161)
- Переменную `heygen` (строка 212)

---

## 2. Запросы к API (отключить вызовы)

### 2.1. Автоматический запуск аватара при загрузке

**Файл**: `app/pages/index.vue`

**Строки 456-471**: Функция `autoStartAvatarIfEnabled()`

- **Оставить код функции**
- **Удалить вызов** в `onMounted()` (строка 485)

**Изменение**:

```typescript
onMounted(async () => {
  // ...
  // УДАЛИТЬ эту строку:
  // await autoStartAvatarIfEnabled();
});
```

---

### 2.2. Запуск аватара при выборе на welcome screen

**Файл**: `app/pages/index.vue`

**Строки 285-288**: Удалить запуск аватара в `handleWelcomeSelect()`

```typescript
// УДАЛИТЬ:
if (chatSettings.avatar && !heygen.isConnected && !heygen.isStarting) {
  await heygen.startSession();
}
```

**Оставить**: Весь остальной код функции

---

### 2.3. Использование аватара для озвучки

**Файл**: `app/pages/index.vue`

**Строки 366-377**: Функция `speakLastMessage()`

- **Изменить логику**: Убрать проверку и использование `heygen.speak()`
- **Оставить**: Использование TTS

**Изменение**:

```typescript
async function speakLastMessage(content: string) {
  if (content && chatSettings.voice) {
    // Убрать проверку аватара, использовать только TTS
    await speakTTS(content);
  }
}
```

**Строки 299-308**: В `handleWelcomeSelect()` - уже использует `speakLastMessage()`, изменения применятся автоматически

**Строки 412-420**: В `onSend()` - уже использует `speakLastMessage()`, изменения применятся автоматически

---

### 2.4. Отключение запросов при переключении аватара

**Файл**: `app/components/AvatarVoiceControls.vue`

**Строки 63-81**: Функция `toggleAvatar()`

- **Оставить код функции полностью** - никаких изменений не требуется
- **Функция не будет вызываться**, так как кнопка аватара скрыта (см. п. 1.1)
- Код остается для возможного использования в будущем

**Примечание**: Поскольку кнопка аватара скрыта через `v-if="false"` или `style="display: none"`, событие `@click="toggleAvatar"` не будет срабатывать, и функция не будет вызываться. Код функции можно оставить без изменений.

---

### 2.5. Остановка аватара при возврате на welcome screen

**Файл**: `app/pages/index.vue`

**Строки 540-562**: Watch на `showWelcomeScreen`

- **Удалить остановку HeyGen сессии** (строки 549-559)

**Изменение**:

```typescript
watch(
  () => showWelcomeScreen.value,
  async (isWelcomeScreen) => {
    if (isWelcomeScreen) {
      chat.clearMessages();
      // УДАЛИТЬ блок остановки HeyGen:
      // if (heygen.isConnected || heygen.isStarting) {
      //   await heygen.stopSession();
      // }
    }
  }
);
```

---

### 2.6. Остановка аватара при размонтировании

**Файл**: `app/pages/index.vue`

**Строки 488-501**: `onBeforeUnmount()`

- **Удалить остановку HeyGen** (строка 496)

**Изменение**:

```typescript
onBeforeUnmount(() => {
  if (chat.therapySessionId && !chat.isEndingSession) {
    void chat.endTherapySession();
  }
  stop();
  // УДАЛИТЬ: heygen.stopSession();

  const subscriptionStore = useSubscriptionStore();
  subscriptionStore.invalidateCache();
});
```

---

## 3. Кодовая база (оставить без изменений)

### 3.1. Компоненты

- ✅ `app/components/AvatarVoiceControls.vue` - оставить файл
- ✅ `app/components/HeyGenPlayer.vue` - оставить файл

### 3.2. Stores

- ✅ `app/stores/heygen.ts` - оставить весь код
- ✅ `app/stores/chatSettings.ts` - оставить поле `avatar` в state

### 3.3. API Endpoints

- ✅ `server/api/heygen/*` - все endpoints остаются
- ✅ `server/api/settings/chat.patch.ts` - поддержка поля `avatar` остается

### 3.4. Другие файлы

- ✅ `app/stores/chat.ts` - методы `resetAvatarIdleTimeout()`, `clearAvatarIdleTimeout()` остаются
- ✅ `app/plugins/session-finish.client.ts` - логика завершения HeyGen сессий остается
- ✅ Все импорты и зависимости остаются

---

## 4. Проверочный список

После внесения изменений проверить:

- [ ] На главной странице компонент `AvatarVoiceControls` отображается, но кнопка аватара скрыта
- [ ] Кнопка звука в `AvatarVoiceControls` работает корректно
- [ ] На главной странице не отображается HeyGenPlayer
- [ ] При загрузке страницы не отправляется запрос на запуск аватара
- [ ] При выборе на welcome screen не запускается аватар
- [ ] При отправке сообщения не используется аватар для озвучки (только TTS)
- [ ] При возврате на welcome screen не отправляется запрос на остановку аватара
- [ ] При размонтировании страницы не отправляется запрос на остановку аватара
- [ ] Все компоненты и stores остались в проекте
- [ ] Код компилируется без ошибок
- [ ] Линтер не выдает ошибок

---

## 5. Примечания

1. **Кнопка звука**: Остается рабочей, управляет только TTS озвучкой
2. **Настройки**: Поле `avatar` в `chatSettings` остается, но не используется в UI
3. **Обратная совместимость**: При необходимости можно быстро вернуть функционал, раскомментировав код
4. **Тестирование**: После изменений протестировать основной флоу чата без аватара
