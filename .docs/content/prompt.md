Используй скилл `mentala-program-designer`.

Твоя задача — заново сгенерировать полноценный markdown-файл для сада `program_self_kindness_21.md` с нуля, без опоры на старую single-action версию. Старый контент файла считать legacy и не использовать.

Обязательные источники и ориентиры:

- `.docs/content/program_calm_anxiety_30_v5.md` — эталон актуальной структуры, multi-action модели, tone of voice и runtime-совместимости.
- `.docs/content/program_self_kindness_21.md` — целевой файл, который нужно полностью заполнить заново.
- При необходимости учитывай `.docs/retention/retention_long_term_strategy.md` как источник правил линейного пути, 2 шагов в день и текущей модели садов.

Что нужно сделать:

- Сгенерировать полный, готовый к использованию markdown для `program_self_kindness_21.md`.
- Программа: 21 шаг, растение `peony`.
- Структура должна соответствовать текущей модели первого сада: 5 UI-глав, explicit `actions[]`, первый intro-action (`guided_steps` с `formKind: 'step_intro'`), `miniArticle` там, где нужна психообразовательная опора, weekly-check на ключевых точках, 2-5 actions на шаг.
- `mood_checkin` не считать содержательной практикой и не делать обязательным шаблоном каждого шага.
- Использовать только доказательные подходы и корректную психотерапевтическую рамку: самосострадание, терапия принятия и ответственности, терапия, сфокусированная на сострадании, медитация доброжелательности, когнитивно-поведенческая терапия.
- Поддерживать текущий темп: максимум 2 новых шага в день.
- Программа должна быть совместима с текущим линейным путём садов: после `calm_anxiety_30`, без выбора пользователем следующего сада.
- «Мысль дня» должна быть связана именно с текущим садом и его темой, а не быть общей мотивационной цитатой.
- Не используй старую single-action модель. Не используй псевдо-клинические или чрезмерно абстрактные формулировки.
- Пользовательские тексты должны быть на русском языке. Не используй англоязычные аббревиатуры и названия методов вроде CBT, ACT, CFT, MSC, медитация доброжелательности, PMR, guided self-help, committed action, compassionate reframe, repair-plan или repair-step. Если метод нужно назвать, пиши полное русское название и коротко объясняй смысл.
- Английские технические идентификаторы допустимы только внутри `type`, `formKind`, `template`, `id`, `idSuffix` и `sourceNotes`, но не в заголовках, вопросах, подсказках, вариантах выбора, miniArticle и тексте для пользователя.
- Любую родозависимую форму в тексте для пользователя оформляй разметкой `{мужская|женская}` (пол известен и подставляется на лету): `Ты {прошёл|прошла}`, `Что ты {заметил|заметила}?`, `«я {должен|должна} справиться {сам|сама}»`. Никакого голого мужского рода и никаких скобок «(а)». Формы без зависимости от рода (настоящее время, инфинитив, существительное) и согласование с существительным («мысль появилась») не размечай. См. раздел «Гендер» в скилле `mentala-program-designer`.
- Для сложных форм, где пользователь должен разложить ситуацию, мысль, фразу, план или итог, добавляй `helpHint` с коротким объяснением и 2-3 примерами. Не добавляй подсказку к простым очевидным выборам.
- В action с типом `meditation` текст (`prompt`/`title`) не должен заставлять пользователя что-то вспоминать, анализировать, выбирать «правильный» ответ или думать о другом контенте программы — это уводит из медитации. Давай только спокойное медитативное наставление: дыхание, тело, мягкое внимание к текущему моменту, доброжелательное присутствие. Аналитика, вспоминание прошлых шагов и любые решения выносятся в отдельные actions после медитации (`structured_form`, `journal_entry`, `guided_steps` и т.п.), а не в саму медитацию.

Нужно выдать:

- описание сада;
- методическую базу;
- структуру по главам;
- полный список 21 шага;
- для каждого шага: title, intro, miniArticle при необходимости, actions, source notes;
- сводную таблицу в конце.

При создании контента и программы используй проверенные источники и доказанные терапии.
